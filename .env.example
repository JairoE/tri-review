# Provider API keys. tri-review needs at least two of the three to produce a review.
OPENAI_API_KEY=your_openai_key
ANTHROPIC_API_KEY=your_anthropic_key
GOOGLE_API_KEY=your_google_key

# PR access uses the `gh` CLI, not a token -- run `gh auth login` once instead.
